import { createTheme } from "@material-ui/core";
export const Theme = createTheme({
  palette: {
    primary: {
      light: "#e9323bf0",
      main: "#e40913",
      dark: "#a2272df0",
      contrastText: "#fbb512",
    },
    secondary: {
      light: "#ffef8c",
      main: "#00b0ff",
      dark: "#fbb512",
      contrastText: "#000",
    },
  },
});
